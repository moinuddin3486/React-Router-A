import React from 'react'

const Contact = (props) => {
  return (
    <div>
      <h1 style={{ color: props.mode === "dark" ? "white" : "black" }}>Contact page..</h1>
    </div>
  )
}

export default Contact
